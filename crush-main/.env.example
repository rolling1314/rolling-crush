# Crush 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改配置
# 此文件会被 godotenv 自动加载

# JWT 密钥 - HTTP 和 WebSocket 服务必须使用相同的密钥
# 重要：在生产环境中请使用强随机密钥
JWT_SECRET=your-secure-jwt-secret-here

# 应用环境 (development 或 production)
APP_ENV=development

# 服务器端口配置
HTTP_PORT=8001
WS_PORT=8002

# 调试模式
CRUSH_DEBUG=false

# 数据库配置 (可选，会覆盖 config.yaml 中的配置)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=crush
# POSTGRES_PASSWORD=123456
# POSTGRES_DB=crush
# POSTGRES_SSLMODE=disable

# Redis 配置 (可选)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=123456
# REDIS_DB=0

# MinIO 配置 (可选)
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin123
# MINIO_BUCKET=crush-images

# Sandbox 配置 (可选)
# SANDBOX_BASE_URL=http://localhost:8888
